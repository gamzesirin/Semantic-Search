# Canlı (production) build ayarları — `npm run build` bunu otomatik okur.
# Frontend ve backend aynı domainde (nginx /api'yi backend'e yönlendiriyor),
# o yüzden göreli /api yeterli; domain sabit kodlamaya gerek yok.
REACT_APP_API_BASE=/api
